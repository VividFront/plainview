<div class="relative inline-block {{ class }}">
  <a href="{{ url }}" 
    class="relative inline-block text-primary py-2 px-5 rounded-button font-bold border-primary border" 
    target="{%- if target -%}{{ target }}{%- else -%}_self{%- endif -%}">{{ text }}</a>
</div>
